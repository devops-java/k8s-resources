apiVersion: v1
kind: Pod
metadata:
  name: spring-boot-web
  namespace: development
spec:
  containers:
    - image: rkp201/k8s-spring-app:1.3
      name: spring-boot-web
      ports:
        - containerPort: 8080
          protocol: TCP
